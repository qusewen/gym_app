name: Deploy Fullstack to Dev

on:
  push:
    branches: [dev]

jobs:
  frontend-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pages: write
      id-token: write

    defaults:
      run:
        working-directory: ./front

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'yarn'
          cache-dependency-path: 'front/yarn.lock'

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Build project
        run: yarn build
        env:
          VITE_BASE_URL: /${{ github.event.repository.name }}/dev/

      - name: Setup GitHub Pages
        uses: actions/configure-pages@v4

      - name: Upload build artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./front/dist

      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v4

  # backend-deploy:
  #   runs-on: ubuntu-latest
  #   defaults:
  #     run:
  #       working-directory: ./back
  #
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4
  #
  #     - name: Setup Node.js
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: '18'
  #         cache: 'npm'
  #         cache-dependency-path: 'back/package-lock.json'
  #
  #     - name: Install dependencies
  #       run: npm ci
  #
  #     - name: Run tests
  #       run: npm test
  #
  #     - name: Build backend
  #       run: npm run build
  #
  #     - name: Deploy backend
  #       run: |
  #         echo "Здесь будет деплой бекенда"
  #         # Логика деплоя бекенда (Railway, Heroku, VPS, etc.)